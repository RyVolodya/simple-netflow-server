# Used only when the users table is empty (first start).
ADMIN_USERNAME=admin
ADMIN_PASSWORD=netflow
SESSION_HOURS=24

# Storage defaults for fresh installs
RAW_RETENTION_HOURS=12
STATS_RETENTION_HOURS=720
